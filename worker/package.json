{
  "name": "ocuparche-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply ocuparche --local",
    "db:migrate:remote": "wrangler d1 migrations apply ocuparche --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5",
    "typescript": "^5.6.0",
    "wrangler": "^4.129.1"
  },
  "dependencies": {
    "@pushforge/builder": "^2.0.5"
  }
}
